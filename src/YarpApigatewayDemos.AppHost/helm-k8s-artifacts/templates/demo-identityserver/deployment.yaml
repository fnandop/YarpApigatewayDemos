---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "demo-identityserver-deployment"
  labels:
    app.kubernetes.io/name: "yarpapigatewaydemos-apphost"
    app.kubernetes.io/component: "demo-identityserver"
    app.kubernetes.io/instance: "{{.Release.Name}}"
spec:
  template:
    metadata:
      labels:
        app.kubernetes.io/name: "yarpapigatewaydemos-apphost"
        app.kubernetes.io/component: "demo-identityserver"
        app.kubernetes.io/instance: "{{.Release.Name}}"
    spec:
      containers:
        - image: "{{ .Values.parameters.demo_identityserver.demo_identityserver_image }}"
          name: "demo-identityserver"
          envFrom:
            - configMapRef:
                name: "demo-identityserver-config"
          ports:
            - name: "http"
              protocol: "TCP"
              containerPort: {{ .Values.parameters.demo_identityserver.port_http }}
            - name: "https"
              protocol: "TCP"
              containerPort: {{ .Values.parameters.demo_identityserver.port_https }}
          imagePullPolicy: "IfNotPresent"
  selector:
    matchLabels:
      app.kubernetes.io/name: "yarpapigatewaydemos-apphost"
      app.kubernetes.io/component: "demo-identityserver"
      app.kubernetes.io/instance: "{{.Release.Name}}"
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: "RollingUpdate"
