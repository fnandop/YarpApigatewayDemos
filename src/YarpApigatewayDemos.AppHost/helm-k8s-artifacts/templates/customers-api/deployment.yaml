---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "customers-api-deployment"
  labels:
    app.kubernetes.io/name: "yarpapigatewaydemos-apphost"
    app.kubernetes.io/component: "customers-api"
    app.kubernetes.io/instance: "{{.Release.Name}}"
spec:
  template:
    metadata:
      labels:
        app.kubernetes.io/name: "yarpapigatewaydemos-apphost"
        app.kubernetes.io/component: "customers-api"
        app.kubernetes.io/instance: "{{.Release.Name}}"
    spec:
      containers:
        - image: "{{ .Values.parameters.customers_api.customers_api_image }}"
          name: "customers-api"
          envFrom:
            - configMapRef:
                name: "customers-api-config"
          ports:
            - name: "http"
              protocol: "TCP"
              containerPort:  {{ .Values.parameters.customers_api.port_http }}
            - name: "https"
              protocol: "TCP"
              containerPort:  {{ .Values.parameters.customers_api.port_https }}
          imagePullPolicy: "IfNotPresent"
  selector:
    matchLabels:
      app.kubernetes.io/name: "yarpapigatewaydemos-apphost"
      app.kubernetes.io/component: "customers-api"
      app.kubernetes.io/instance: "{{.Release.Name}}"
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: "RollingUpdate"
