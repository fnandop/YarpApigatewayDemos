---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: customers-api
  labels:
    app: customers-api
spec:
  minReadySeconds: 60
  replicas: 1
  selector:
    matchLabels:
      app: customers-api
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: customers-api
    spec:
      containers:
      - name: customers-api
        image: fnandop/customers-api:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8080
        - name: https
          containerPort: 8443
        envFrom:
        - configMapRef:
            name: customers-api-env
      terminationGracePeriodSeconds: 180