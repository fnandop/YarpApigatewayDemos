---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "reverse-proxy-service"
  labels:
    app.kubernetes.io/name: "yarpapigatewaydemos-apphost"
    app.kubernetes.io/component: "reverse-proxy"
    app.kubernetes.io/instance: "{{.Release.Name}}"
spec:
  type: "ClusterIP"
  selector:
    app.kubernetes.io/name: "yarpapigatewaydemos-apphost"
    app.kubernetes.io/component: "reverse-proxy"
    app.kubernetes.io/instance: "{{.Release.Name}}"
  ports:
    - name: "http"
      protocol: "TCP"
      port: {{ .Values.parameters.reverse_proxy.port_http }}
      targetPort:  {{ .Values.parameters.reverse_proxy.port_http }}
    - name: "https"
      protocol: "TCP"
      port: {{ .Values.parameters.reverse_proxy.port_https }}
      targetPort:  {{ .Values.parameters.reverse_proxy.port_https }}
